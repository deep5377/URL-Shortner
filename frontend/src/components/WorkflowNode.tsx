import { motion } from "framer-motion";
interface Props { label: string; icon: string; status: string; meta?: string; compact?: boolean; }
export function WorkflowNode({ label, icon, status, meta, compact }: Props) { const state = status.toLowerCase(); const display = state === "waiting_approval" ? "Waiting approval" : state === "safe_stop" ? "Safe stop" : state === "retrying" ? "Retrying" : state; return <motion.div layout className={`workflow-node ${state} ${compact ? "compact" : ""}`}><div className="node-icon">{state === "passed" || state === "complete" ? "✓" : state === "safe_stop" || state === "failed" ? "!" : icon}</div><div className="node-copy"><strong>{label}</strong><span>{meta || display}</span></div>{state === "running" || state === "retrying" ? <span className="node-spinner" /> : null}</motion.div>; }