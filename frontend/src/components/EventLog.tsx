import { AnimatePresence, motion } from "framer-motion";
import type { WorkflowEvent } from "../types";
export function EventLog({ events }: { events: WorkflowEvent[] }) { return <section className="panel event-panel"><div className="panel-heading"><div><span className="eyebrow">TRACE / LIVE</span><h3>Event timeline</h3></div><span className="event-count">{events.length} events</span></div><div className="event-list"><AnimatePresence initial={false}>{[...events].reverse().map((event, index) => <motion.div className="event" key={`${event.timestamp}-${event.action}-${index}`} initial={{ opacity: 0, x: 10 }} animate={{ opacity: 1, x: 0 }}><div className={`event-mark ${event.status.toLowerCase()}`} /> <div className="event-body"><div><strong>{event.action.replaceAll("_", " ")}</strong><span className="event-status">{event.status}</span></div><p>{event.agent} · {event.stage}</p>{event.detail && <small>{event.detail}</small>}</div><time>{new Date(event.timestamp).toLocaleTimeString([], { hour: "2-digit", minute: "2-digit" })}</time></motion.div>)}</AnimatePresence>{events.length === 0 && <div className="empty">Events will appear as the workflow executes.</div>}</div></section>; }