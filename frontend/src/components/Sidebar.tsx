interface Props { active: string; onNavigate: (value: string) => void; }
const items = [{ id: "dashboard", icon: "◈", label: "Dashboard" }, { id: "new-run", icon: "+", label: "New Run" }, { id: "workflows", icon: "◎", label: "Workflows" }, { id: "url-demo", icon: "↗", label: "URL Demo" }, { id: "metrics", icon: "▥", label: "Metrics" }];
export function Sidebar({ active, onNavigate }: Props) { return <aside className="sidebar"><div className="brand"><span className="brand-mark">✦</span><span>AGENTIC<br /><b>ENGINEERING</b></span></div><nav>{items.map(item => <button title={item.label} aria-label={item.label} className={active === item.id ? "nav-item active" : "nav-item"} onClick={() => onNavigate(item.id)} key={item.id}><span className="nav-icon">{item.icon}</span>{item.label}{item.id === "workflows" && <span className="nav-dot" />}</button>)}</nav><div className="sidebar-bottom"><div className="system-line"><span className="pulse" /> Ready</div></div></aside>; }